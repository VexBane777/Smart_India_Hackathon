# Required by flutter_onnxruntime (see its pub.dev README) if/when
# minification is ever turned on for a release build — this project's
# current release build doesn't enable it, but keep this ready.
-keep class ai.onnxruntime.** { *; }
