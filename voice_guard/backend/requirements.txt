fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.0
numpy>=1.24
# Scoring with the deployed artifact (assets/models/voice_detector.onnx) — see
# model_backend.py. If this is absent the API still runs: /v1/model-info reports
# loaded=false with the reason and the legacy heuristic path keeps serving.
onnxruntime>=1.17
# Test-only: fastapi.testclient needs httpx. Listed here so `pip install -r
# requirements.txt` is enough to run the suite in voice_guard/backend/.
httpx>=0.27
pytest>=8.0

